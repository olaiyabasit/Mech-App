[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
    "python build_tailwind.py",
    "python manage.py collectstatic --noinput -v 2"
]

[start]
cmd = "gunicorn winki_project.wsgi --log-file -"
